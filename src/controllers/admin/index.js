const authController = require('./auth');
const profileController = require('./profile');
const userController = require('./user');
const vehicleTypeController = require('./vehicle-type');

module.exports = {
  authController,
  profileController,
  userController,
  vehicleTypeController
}