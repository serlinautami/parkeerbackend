const authController = require('./auth');
// const profileController = require('./profile');
// const parkirControlller = require('./parkir');
const userController = require('./user');

module.exports = {
  authController,
  // profileController,
  // parkirControlller,
  userController
}